package chapter5LazyEvaluate

object InfiniteStream {


//    def constant[A](a: A): Stream[A] = {
////        val ones: Stream[A] = Stream.cons(a, ones)
//
////        ones
//    }


//    def from(n: Int): Stream[Int] = {
//        val m = n - 1
//        val ones: Stream[Int] = Stream.cons(n + 1 , ones)
//        ones
//    }

    def main(args: Array[String]): Unit = {
//        println(from(1).toList(10000))
    }

}
